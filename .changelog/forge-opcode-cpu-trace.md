---
forge: minor
---

Added opt-in thread CPU timing to Forge call traces with `--cpu-trace`, CPU-weighted flamecharts with `--cpu-flamechart`, and per-opcode timing when `--cpu-trace` is combined with `--opcodes`.
